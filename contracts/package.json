{
  "name": "ai-yield-agent-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for AI Yield Agent on Monad testnet",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run deploy.js --network monad-testnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-waffle": "^2.0.6",
    "hardhat": "^2.19.0",
    "ethers": "^5.7.2"
  }
}